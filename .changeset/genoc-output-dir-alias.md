---
'genoc': minor
---

Add `-o` as a short alias for the `--output-dir` CLI flag: `genoc ./spec.yaml -o ./src/api` now works alongside the long form, including when overriding a config file's `outputDir`.
